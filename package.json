{
  "name": "hardweb-pos",
  "version": "0.1.0",
  "private": true,
  "description": "Restoran uchun lokal POS tizimi (desktop) — Electron + NestJS + PostgreSQL",
  "workspaces": [
    "shared",
    "server",
    "desktop",
    "cloud"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:desktop": "npm run dev --workspace=desktop",
    "dev:desktop:mock": "npm run dev:mock --workspace=desktop",
    "dev:cloud": "npm run dev --workspace=cloud",
    "build:shared": "npm run build --workspace=shared"
  },
  "engines": {
    "node": ">=20"
  }
}
