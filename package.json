{
  "name": "hardweb-pos",
  "version": "0.1.0",
  "private": true,
  "description": "Restoran uchun lokal POS tizimi (desktop) — Electron + NestJS + PostgreSQL",
  "workspaces": [
    "shared",
    "server",
    "desktop"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:desktop": "npm run dev --workspace=desktop",
    "build:shared": "npm run build --workspace=shared"
  },
  "engines": {
    "node": ">=20"
  }
}
